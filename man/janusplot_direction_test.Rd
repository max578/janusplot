% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/direction-test.R
\name{janusplot_direction_test}
\alias{janusplot_direction_test}
\title{Test whether the residual-independence condition licenses a direction}
\usage{
janusplot_direction_test(
  data,
  x,
  y,
  k = -1L,
  bs = "tp",
  method = NULL,
  alpha = 0.05,
  n_permutations = 500L,
  linear_tol = 0.01,
  gaussian_alpha = 0.01,
  seed = NULL
)
}
\arguments{
\item{data}{A data frame containing both variables.}

\item{x}{Character. Column name of the first variable -- the putative
cause under the forward hypothesis.}

\item{y}{Character. Column name of the second variable -- the putative
effect under the forward hypothesis.}

\item{k}{Basis dimension passed to \code{\link[mgcv:s]{mgcv::s()}}. Default \code{-1L} lets
\code{mgcv} choose.}

\item{bs}{Character. Spline basis passed to \code{\link[mgcv:s]{mgcv::s()}}. Default
\code{"tp"} (thin-plate regression spline).}

\item{method}{Smoothing-parameter selection method passed to
\code{\link[mgcv:gam]{mgcv::gam()}}. \code{NULL} (default) uses \code{"REML"}.}

\item{alpha}{Numeric in \verb{(0, 1)}. Significance level for the two
residual-independence tests. Residuals are called independent when
the HSIC p-value exceeds \code{alpha}. Default \code{0.05}.}

\item{n_permutations}{Integer. Permutations used to build each HSIC
null distribution. Must be at least \code{99} -- the smallest attainable
p-value is \code{1 / (n_permutations + 1)}. Default \code{500L}.}

\item{linear_tol}{Numeric in \verb{(0, 1)}. A fitted smooth counts as a
straight line when \code{1 - cor(fitted, x)^2} falls below this value.
Default \code{0.01}.}

\item{gaussian_alpha}{Numeric in \verb{(0, 1)}. Significance level for the
four Shapiro-Wilk normality tests used by the linear-Gaussian
detector. Default \code{0.01}.}

\item{seed}{Integer or \code{NULL}. Seed for the HSIC permutation draws.
The reverse direction is seeded at \code{seed + 1} so the two null
distributions are not drawn from the same permutation sequence.}
}
\value{
A named list:
\describe{
\item{\code{verdict}}{One of \code{"forward"} (\code{x} drives \code{y}),
\code{"reverse"} (\code{y} drives \code{x}), or \code{"undecided"}.}
\item{\code{reason_code}}{Machine-readable ground for the verdict:
\code{"forward_only"}, \code{"reverse_only"}, \code{"both_independent"},
\code{"neither_independent"}, or \code{"linear_gaussian"}.}
\item{\code{reason}}{One-paragraph statement of the same ground.}
\item{\code{x}, \code{y}}{The column names, as supplied.}
\item{\code{forward}, \code{reverse}}{The two hypotheses as printable
strings, e.g. \code{"x -> y"}.}
\item{\code{p_forward}, \code{p_reverse}}{HSIC permutation p-values for
residual-versus-cause independence in each direction.}
\item{\code{statistic_forward}, \code{statistic_reverse}}{The two observed
HSIC statistics.}
\item{\code{independent_forward}, \code{independent_reverse}}{Whether each
p-value exceeds \code{alpha}.}
\item{\code{edf_forward}, \code{edf_reverse}}{Effective degrees of freedom
of each fitted smooth.}
\item{\code{curvature_forward}, \code{curvature_reverse}}{Departure of each
smooth from a straight line; see Details.}
\item{\code{normality_p}}{Named numeric vector of the four
Shapiro-Wilk p-values (\code{x}, \code{y}, \code{residuals_forward},
\code{residuals_reverse}).}
\item{\code{linear_gaussian}}{\code{TRUE} when the non-identifiable
linear-Gaussian case was detected.}
\item{\code{n}}{Complete cases used.}
\item{\code{alpha}, \code{gaussian_alpha}, \code{linear_tol},
\code{n_permutations}}{The settings the verdict was reached under.}
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

\code{\link[=janusplot]{janusplot()}} renders a pair twice -- \code{y ~ s(x)} in one triangle and
\code{x ~ s(y)} in the other -- and summarises the difference between the
two fits as an asymmetry index. That index is a \emph{diagnostic}: it says
the two directions of fit are not interchangeable, not that either one
is the causal direction.

This function runs the test that turns the picture into a claim. Under
the additive-noise model \code{y = f(x) + e} with \code{e} independent of \code{x},
the model holds in at most one direction for all but a small set of
exceptional joint distributions. So a direction is supported when
\itemize{
\item the residuals of the fit in that direction are independent of the
putative cause, \strong{and}
\item the residuals of the fit in the opposite direction are not.
}

Independence is assessed with the kernel (HSIC) permutation test in
\code{kernR::hsic_test()}. Anything other than the clean one-sided pattern
-- both directions admitting an additive-noise model, neither doing
so, or the known non-identifiable linear-Gaussian case -- returns
\code{"undecided"}. The smaller of the two p-values is never read as a
direction.
}
\details{
\subsection{The linear-Gaussian exception}{

Identifiability fails exactly when the relationship is linear and both
the cause and the noise are Gaussian: a bivariate Gaussian is
reproduced perfectly by a linear model with Gaussian noise in either
direction, so the residual-independence condition holds both ways and
carries no directional information. This case is detected before the
two independence tests are read, and returns \code{"undecided"} with
\code{reason_code = "linear_gaussian"}. It is flagged when \emph{both} of the
following hold:
\itemize{
\item \strong{Linearity.} Each fitted smooth departs from a straight line by
less than \code{linear_tol}, measured as \code{1 - cor(fitted, x)^2} -- the
share of the fitted variation that no straight line in the predictor
can reproduce.
\item \strong{Gaussianity.} Shapiro-Wilk applied to \code{x}, \code{y}, and both residual
vectors fails to reject normality at \code{gaussian_alpha} in all four
cases.
}

\code{gaussian_alpha} defaults to \code{0.01}, not \code{0.05}: the consequence of
the flag is abstention, so the threshold is set to withhold a
directional claim unless the data argue clearly against Gaussianity.
}

\subsection{Reading the result}{

A \code{"forward"} or \code{"reverse"} verdict states that the condition
licensing that direction holds on this sample. It is not a proof of
causation: the additive-noise model assumes no unobserved common
cause, no feedback, and additive noise, and none of those assumptions
is testable from the pair alone.
}
}
\examples{
if (requireNamespace("kernR", quietly = TRUE)) {
  # x drives y through a non-linear function with non-Gaussian noise:
  # the additive-noise model is identifiable here.
  set.seed(2026L)
  n <- 200L
  x <- stats::runif(n, -3, 3)
  y <- x^3 / 5 + stats::runif(n, -1, 1)
  d <- data.frame(x = x, y = y)

  res <- janusplot_direction_test(d, "x", "y", n_permutations = 199L,
                                  seed = 1L)
  res$verdict      # "forward"
  res$reason
  c(forward = res$p_forward, reverse = res$p_reverse)

  # The same pair read the other way round returns "reverse".
  janusplot_direction_test(d, "y", "x", n_permutations = 199L,
                           seed = 1L)$verdict
}
}
\references{
Hoyer, P. O., Janzing, D., Mooij, J. M., Peters, J., &
Scholkopf, B. (2009). Nonlinear causal discovery with additive
noise models. \emph{Advances in Neural Information Processing Systems},
\strong{21}, 689-696.

Gretton, A., Fukumizu, K., Teo, C. H., Song, L., Scholkopf, B., &
Smola, A. J. (2008). A kernel statistical test of independence.
\emph{Advances in Neural Information Processing Systems}, \strong{20},
585-592.

Shimizu, S., Hoyer, P. O., Hyvarinen, A., & Kerminen, A. (2006). A
linear non-Gaussian acyclic model for causal discovery. \emph{Journal of
Machine Learning Research}, \strong{7}, 2003-2030.
}
\seealso{
\code{\link[=janusplot]{janusplot()}}, \code{\link[=janusplot_data]{janusplot_data()}}.
}
