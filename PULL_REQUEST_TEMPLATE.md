# NA

## Summary

## Checklist

Linked issue: \#

`devtools::document()` run; `NAMESPACE` + `man/` up to date

`devtools::test()` green on macOS/Linux (at least)

`devtools::check(args = "--as-cran")` clean (or NOTEs explained below)

`NEWS.md` bullet added under `# janusplot (development version)`

If public API changed: `@param` / `@returns` / executable `@examples` /
`@family` or `@seealso` all present

## Remaining check output

Click to expand

    # paste R CMD check --as-cran summary here
